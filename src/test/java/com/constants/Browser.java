package com.constants;

public enum Browser {
CHROME,EDGE,FIREFOX
}

//in enum all values are public static final
// as its upper case its constant so constant we write in final
// with final always static needs to be there

